fix: Corrige tela em branco no Laboratório e Dashboard status

- Importação do componente `Label` em `Laboratory.tsx` que estava faltando, causando uma quebra de renderização na aba de pesquisa.
- Tratamento para pedidos sem data de criação em `Laboratory.tsx`.
- Corrige contagem de pedidos no `Dashboard.tsx` para utilizar e puxar `lab_status` do banco, evitando contagem zerada/incorreta das métricas do laboratório.
